import React from 'react';

const ProgressBar = (props) => {
    console.log(props);
    
    return (
        <div>
            <h3>{props.title}</h3>
        </div>
    );
};

export default ProgressBar;